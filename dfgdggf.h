#include <iostream>
#include <string>
using namesapce std;

int harsh(string key)
{
    int sum = 0;
    int index;
    for(int i = 0; i <=key.length; i++)
    {
        sum += (int).key[i];
    }
    index = sum % 10;
    return index;
}


int HashTable::countBucket(int index ) const 
{
    assert(0 <= index <= SIZE)
    return 
}

int main()
{


}